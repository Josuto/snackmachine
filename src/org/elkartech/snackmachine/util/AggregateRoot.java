package org.elkartech.snackmachine.util;

public abstract class AggregateRoot extends Entity {

    // We could add some versioning info to enable optimistic locking
    // We could add all the domain events that happened to an aggregate during its lifetime
    
}
